import { useEffect, useMemo, useState } from 'react';
import { Archive, CheckCircle2, ChevronDown, Edit3, Filter, Plus, Search, ShieldAlert, X } from 'lucide-react';
import type { BibleProposal, BibleUpdateRun, Chapter, CreateStoryEntityInput, EntityStatus, EntityType, ReviewBibleProposalInput, StoryEntity, StorySourceReference } from '../../types/domain';
import type { StoryRepository } from '../../services/storyRepository';

const typeLabels: Record<EntityType, string> = { character: 'Charakter', relationship: 'Beziehung', place: 'Ort', organization: 'Organisation', world_rule: 'Weltregel', object: 'Gegenstand', event: 'Ereignis', fact: 'Fakt', clue: 'Hinweis', secret: 'Geheimnis', plot_thread: 'Handlungsstrang', retcon: 'Retcon', author_note: 'Autorennotiz' };
const statusLabels: Record<EntityStatus, string> = { confirmed: 'Bestätigt', proposed: 'Vorgeschlagen', uncertain: 'Vermutung', contradicted: 'Widersprochen', retconned: 'Retcon', archived: 'Archiviert' };
const emptyForm = (projectId: string): CreateStoryEntityInput => ({ projectId, name: '', type: 'fact', description: '', status: 'proposed', confidence: 0.7, excerpt: '', authorConfirmed: false, tags: [] });

interface Props { entities: StoryEntity[]; projectId: string; chapters: Chapter[]; initialFilter?: EntityType; repository: StoryRepository; activeRun?: BibleUpdateRun; proposals: BibleProposal[]; onEntityChanged: (entity: StoryEntity) => void; onOpenSourceReference: (reference: StorySourceReference) => void; onReview: (input: ReviewBibleProposalInput) => Promise<void>; onCompleteReview: () => Promise<void>; onCloseReview: () => void; }

export function StoryBibleView({ entities, projectId, chapters, initialFilter, repository, activeRun, proposals, onEntityChanged, onOpenSourceReference, onReview, onCompleteReview, onCloseReview }: Props) {
  const [query, setQuery] = useState('');
  const [filter, setFilter] = useState<EntityType | 'all'>(initialFilter ?? 'all');
  const [showArchived, setShowArchived] = useState(false);
  const [selected, setSelected] = useState(entities[0]?.id);
  const [form, setForm] = useState<CreateStoryEntityInput>(emptyForm(projectId));
  const [editing, setEditing] = useState<StoryEntity | undefined>();
  const [formOpen, setFormOpen] = useState(false);
  const [saving, setSaving] = useState(false);
  const [error, setError] = useState('');
  const filtered = useMemo(() => entities.filter((entity) => (showArchived || entity.status !== 'archived') && (filter === 'all' || entity.type === filter) && `${entity.name} ${entity.description} ${entity.tags.join(' ')}`.toLocaleLowerCase().includes(query.toLocaleLowerCase())), [entities, filter, query, showArchived]);
  const active = entities.find((entity) => entity.id === selected) ?? filtered[0];
  useEffect(() => { if (active && active.id !== selected) setSelected(active.id); }, [active, selected]);
  const openNew = () => { setEditing(undefined); setForm(emptyForm(projectId)); setError(''); setFormOpen(true); };
  const openEdit = (entity: StoryEntity) => { const chapter = chapters.find((item) => item.title === entity.chapter); const scene = chapter?.scenes.find((item) => item.title === entity.scene); setEditing(entity); setForm({ projectId, name: entity.name, type: entity.type, description: entity.description, status: entity.status, confidence: entity.confidence, chapterId: chapter?.id, sceneId: scene?.id, excerpt: entity.source, authorConfirmed: entity.authorConfirmed, tags: entity.tags }); setError(''); setFormOpen(true); };
  const save = async () => { setSaving(true); setError(''); try { const saved = editing ? await repository.updateStoryEntity({ ...form, id: editing.id }) : await repository.createStoryEntity(form); onEntityChanged(saved); setEditing(undefined); setFormOpen(false); } catch (cause) { setError(cause instanceof Error ? cause.message : 'Der Eintrag konnte nicht gespeichert werden.'); } finally { setSaving(false); } };
  const archive = async () => { if (!active) return; setSaving(true); try { onEntityChanged(await repository.archiveStoryEntity(active.id)); } catch (cause) { setError(cause instanceof Error ? cause.message : 'Der Eintrag konnte nicht archiviert werden.'); } finally { setSaving(false); } };
  return <section className="bible-view"><div className="view-heading"><div><span className="eyebrow">KANONISCHE WISSENSBASIS</span><h1>Story Bible</h1><p>Du bestätigst jeden Eintrag selbst. Quellen bleiben mit der Szene verknüpft.</p></div><button className="primary-button" onClick={openNew}><Plus size={16} /> Neuer Eintrag</button></div><div className="bible-toolbar"><div className="search-field"><Search size={15} /><input placeholder="Story Bible durchsuchen …" value={query} onChange={(event) => setQuery(event.target.value)} /></div><div className="filter-select"><Filter size={14} /><select value={filter} onChange={(event) => setFilter(event.target.value as EntityType | 'all')}><option value="all">Alle Kategorien</option>{Object.entries(typeLabels).map(([key, label]) => <option key={key} value={key}>{label}</option>)}</select><ChevronDown size={13} /></div><label className="checkline"><input type="checkbox" checked={showArchived} onChange={(event) => setShowArchived(event.target.checked)} /> Archivierte</label></div><div className="bible-layout"><div className="entity-list"><div className="list-summary"><span>{filtered.length} Einträge</span><span>{entities.filter((entity) => entity.status === 'uncertain' || entity.status === 'proposed').length} offen</span></div>{filtered.map((entity) => <button className={`entity-row ${entity.id === active?.id ? 'active' : ''}`} key={entity.id} onClick={() => setSelected(entity.id)}><span className={`entity-icon ${entity.type}`}>{entity.name.slice(0, 1)}</span><span className="entity-row-main"><strong>{entity.name}</strong><span>{typeLabels[entity.type]} · {entity.origin === 'bible_update' ? 'Bible Update' : 'Manuell'}</span></span><span className={`status-pill ${entity.status === 'confirmed' ? 'green' : entity.status === 'uncertain' ? 'yellow' : 'purple'}`}>{statusLabels[entity.status]}</span></button>)}{!filtered.length && <div className="empty-state">Noch keine passenden Einträge. Lege den ersten an.</div>}</div>{active ? <div className="entity-detail"><div className="detail-top"><div><span className={`status-pill ${active.status === 'confirmed' ? 'green' : active.status === 'uncertain' ? 'yellow' : 'purple'}`}>{statusLabels[active.status]}</span><h2>{active.name}</h2><span className="detail-type">{typeLabels[active.type]} · {active.origin === 'bible_update' ? 'aus Bible Update' : 'manuell'}</span></div><div className="detail-actions"><button className="ghost-button" onClick={() => openEdit(active)}><Edit3 size={14} /> Bearbeiten</button><button className="icon-button" onClick={() => void archive()} title="Eintrag archivieren"><Archive size={16} /></button></div></div><p className="detail-description">{active.description || 'Keine Beschreibung hinterlegt.'}</p><div className="detail-stats"><div><span>Vertrauensstufe</span><strong>{Math.round(active.confidence * 100)}%</strong><div className="progress-line"><span style={{ width: `${active.confidence * 100}%` }} /></div></div><div><span>Autor bestätigt</span><strong>{active.authorConfirmed ? <><CheckCircle2 size={15} /> Ja</> : 'Noch offen'}</strong></div></div><div className="source-card"><div className="source-card-head"><span><ShieldAlert size={15} /> QUELLE</span><span>{active.updatedAt}</span></div><strong>{active.chapter || 'Keine Szene verknüpft'}</strong><span>{active.scene} {active.source ? `· „${active.source.slice(0, 90)}“` : ''}</span>{active.chapter && active.scene && <button className="text-button" onClick={() => { const chapter = chapters.find((item) => item.title === active.chapter); const scene = chapter?.scenes.find((item) => item.title === active.scene); if (chapter && scene) onOpenSourceReference({ id: `entity-source-${active.id}`, projectId, entityId: active.id, chapterId: chapter.id, sceneId: scene.id, excerpt: active.source, createdAt: active.updatedAt }); }}>Zur Textstelle springen <ChevronDown size={14} /></button>}</div><div className="tag-list">{active.tags.map((tag) => <span key={tag}>#{tag}</span>)}</div></div> : <div className="empty-state">Keine Einträge gefunden.</div>}</div>{activeRun && <ReviewModal run={activeRun} proposals={proposals} onReview={onReview} onComplete={onCompleteReview} onClose={onCloseReview} />}{formOpen && <EntityModal form={form} setForm={setForm} editing={Boolean(editing)} chapters={chapters} saving={saving} error={error} onClose={() => { setEditing(undefined); setFormOpen(false); }} onSave={() => void save()} />}</section>;
}

function EntityModal({ form, setForm, editing, chapters, saving, error, onClose, onSave }: { form: CreateStoryEntityInput; setForm: (form: CreateStoryEntityInput) => void; editing: boolean; chapters: Chapter[]; saving: boolean; error: string; onClose: () => void; onSave: () => void }) {
  const scenes = chapters.find((chapter) => chapter.id === form.chapterId)?.scenes ?? [];
  return <div className="modal-backdrop" role="dialog" aria-modal="true"><div className="modal simple-modal bible-form-modal"><div className="modal-head"><div><span className="eyebrow">{editing ? 'STORY BIBLE' : 'NEUER EINTRAG'}</span><h2>{editing ? 'Eintrag bearbeiten' : 'Eintrag anlegen'}</h2></div><button className="icon-button" onClick={onClose} aria-label="Dialog schließen"><X size={18} /></button></div>{error && <div className="save-error" role="alert">{error}</div>}<div className="form-grid"><label className="field-label">Name<input value={form.name} onChange={(event) => setForm({ ...form, name: event.target.value })} autoFocus /></label><label className="field-label">Typ<select value={form.type} onChange={(event) => setForm({ ...form, type: event.target.value as EntityType })}>{Object.entries(typeLabels).map(([key, label]) => <option key={key} value={key}>{label}</option>)}</select></label><label className="field-label">Status<select value={form.status} onChange={(event) => setForm({ ...form, status: event.target.value as EntityStatus })}>{Object.entries(statusLabels).map(([key, label]) => <option key={key} value={key}>{label}</option>)}</select></label><label className="field-label">Vertrauen<input type="number" min="0" max="100" value={Math.round(form.confidence * 100)} onChange={(event) => setForm({ ...form, confidence: Number(event.target.value) / 100 })} /></label><label className="field-label full-field">Beschreibung<textarea rows={3} value={form.description} onChange={(event) => setForm({ ...form, description: event.target.value })} /></label><label className="field-label">Kapitel<select value={form.chapterId ?? ''} onChange={(event) => setForm({ ...form, chapterId: event.target.value || undefined, sceneId: undefined })}><option value="">Keine Quelle</option>{chapters.map((chapter) => <option key={chapter.id} value={chapter.id}>{chapter.title}</option>)}</select></label><label className="field-label">Szene<select value={form.sceneId ?? ''} onChange={(event) => setForm({ ...form, sceneId: event.target.value || undefined })}><option value="">Keine Quelle</option>{scenes.map((scene) => <option key={scene.id} value={scene.id}>{scene.title}</option>)}</select></label><label className="field-label full-field">Quellenausschnitt<textarea rows={2} value={form.excerpt} onChange={(event) => setForm({ ...form, excerpt: event.target.value })} placeholder="Wörtlicher Ausschnitt aus dem Manuskript …" /></label><label className="field-label full-field">Tags<input value={form.tags.join(', ')} onChange={(event) => setForm({ ...form, tags: event.target.value.split(',').map((tag) => tag.trim()).filter(Boolean) })} placeholder="z. B. Hauptfigur, Wissen" /></label><label className="checkline full-field"><input type="checkbox" checked={form.authorConfirmed} onChange={(event) => setForm({ ...form, authorConfirmed: event.target.checked })} /> Vom Autor bestätigt</label></div><div className="modal-actions"><button className="ghost-button" onClick={onClose}>Abbrechen</button><button className="primary-button" onClick={onSave} disabled={saving || !form.name.trim()}>{saving ? 'Speichert …' : 'Speichern'}</button></div></div></div>;
}

function ReviewModal({ proposals, onReview, onComplete, onClose }: { run: BibleUpdateRun; proposals: BibleProposal[]; onReview: (input: ReviewBibleProposalInput) => Promise<void>; onComplete: () => Promise<void>; onClose: () => void }) {
  const [busy, setBusy] = useState('');
  const review = async (proposal: BibleProposal, reviewStatus: ReviewBibleProposalInput['reviewStatus'], classification?: ReviewBibleProposalInput['classification'], candidateStatus?: EntityStatus) => { setBusy(proposal.id); try { await onReview({ proposalId: proposal.id, reviewStatus, classification, candidateStatus }); } finally { setBusy(''); } };
  const editAndAccept = async (proposal: BibleProposal) => { const name = window.prompt('Name des Vorschlags', proposal.candidateName); if (name === null) return; const description = window.prompt('Beschreibung des Vorschlags', proposal.candidateDescription); if (description === null) return; setBusy(proposal.id); try { await onReview({ proposalId: proposal.id, reviewStatus: 'edited', candidateName: name.trim() || proposal.candidateName, candidateDescription: description.trim() || proposal.candidateDescription }); } finally { setBusy(''); } };
  const acceptSafeFacts = async () => { setBusy('bulk'); for (const proposal of proposals.filter((item) => item.reviewStatus === 'pending' && item.classification === 'observable_fact')) await onReview({ proposalId: proposal.id, reviewStatus: 'accepted' }); setBusy(''); };
  const rejectRest = async () => { setBusy('bulk'); for (const proposal of proposals.filter((item) => item.reviewStatus === 'pending')) await onReview({ proposalId: proposal.id, reviewStatus: 'rejected' }); setBusy(''); };
  const pending = proposals.filter((proposal) => proposal.reviewStatus === 'pending');
  return <div className="modal-backdrop" role="dialog" aria-modal="true"><div className="modal simple-modal bible-review-modal"><div className="modal-head"><div><span className="eyebrow">LOKALER PROTOTYP-EXTRACTOR</span><h2>Vorschläge prüfen</h2><p className="modal-intro">Nichts wird automatisch zum Kanon. Du entscheidest jeden Vorschlag.</p></div><button className="icon-button" onClick={onClose} aria-label="Review schließen"><X size={18} /></button></div><div className="proposal-summary simple-summary"><div><strong>{proposals.filter((p) => p.classification === 'observable_fact').length}</strong><span>beobachtbare Fakten</span></div><div><strong>{proposals.filter((p) => p.classification === 'interpretation').length}</strong><span>Interpretationen</span></div><div><strong>{proposals.filter((p) => p.classification === 'open_question').length}</strong><span>offene Fragen</span></div><div><strong>{proposals.filter((p) => p.classification === 'possible_contradiction').length}</strong><span>Widersprüche</span></div></div><div className="review-bulk-actions"><button className="ghost-button" disabled={busy !== ''} onClick={() => void acceptSafeFacts()}>Sichere Fakten übernehmen</button><button className="text-button" disabled={busy !== ''} onClick={() => void rejectRest()}>Rest verwerfen</button></div><div className="review-list">{proposals.map((proposal) => <article className={`review-card ${proposal.reviewStatus}`} key={proposal.id}><div className="review-card-main"><span className="status-pill purple">{proposal.classification.replaceAll('_', ' ')}</span><h3>{proposal.candidateName}</h3><p>{proposal.candidateDescription}</p><small>„{proposal.evidenceExcerpt || 'Keine direkte Passage'}“ · {proposal.reason}</small></div>{proposal.reviewStatus === 'pending' ? <div className="review-actions"><button className="primary-button" disabled={busy === proposal.id || busy === 'bulk'} onClick={() => void review(proposal, 'accepted')}>Übernehmen</button><button className="ghost-button" disabled={busy === proposal.id || busy === 'bulk'} onClick={() => void editAndAccept(proposal)}>Bearbeiten</button><button className="ghost-button" disabled={busy === proposal.id || busy === 'bulk'} onClick={() => void review(proposal, 'accepted', 'interpretation', 'uncertain')}>Vermutung</button><button className="text-button" disabled={busy === proposal.id || busy === 'bulk'} onClick={() => void review(proposal, 'rejected')}>Verwerfen</button></div> : <span className="reviewed-label">{proposal.reviewStatus === 'rejected' ? 'Verworfen' : proposal.reviewStatus === 'edited' ? 'Bearbeitet übernommen' : 'Übernommen'}</span>}</article>)}</div><div className="modal-actions"><button className="ghost-button" onClick={onClose}>Später prüfen</button><button className="primary-button" disabled={pending.length > 0} onClick={() => void onComplete()}>Review abschließen</button></div></div></div>;
}

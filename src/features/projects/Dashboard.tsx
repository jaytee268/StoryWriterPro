import { ArrowRight, BookOpen, Clock3, Upload } from 'lucide-react';
import type { Project } from '../../types/domain';

export function Dashboard({ project, onOpen, onImport }: { project: Project; onOpen: () => void; onImport: () => void }) { return <section className="dashboard simple-dashboard"><div className="simple-welcome"><span className="eyebrow">WILLKOMMEN ZURÜCK</span><h1>Was möchtest du heute schreiben?</h1><p>Öffne dein Manuskript und arbeite genau dort weiter, wo du aufgehört hast.</p></div><button className="open-project-card" onClick={onOpen}><span className="book-cover">Z</span><span className="open-project-copy"><span className="eyebrow">LETZTES PROJEKT</span><strong>{project.title}</strong><span>{project.wordCount.toLocaleString('de-DE')} Wörter · zuletzt bearbeitet {project.updatedAt}</span></span><span className="open-project-arrow"><ArrowRight size={23} /></span></button><div className="simple-actions"><button className="big-action primary" onClick={onOpen}><BookOpen size={22} /><span><strong>Weiterschreiben</strong><small>Zur letzten Szene springen</small></span><ArrowRight size={20} /></button><button className="big-action" onClick={onImport}><Upload size={22} /><span><strong>Manuskript importieren</strong><small>TXT oder Markdown öffnen</small></span><ArrowRight size={20} /></button></div><div className="simple-note"><Clock3 size={17} /><span>Dein Projekt wird automatisch lokal gespeichert. Keine Cloud, kein Konto, kein kompliziertes Setup.</span></div></section>; }
